export default function Loading() {
    return (
        <div className="fixed inset-0 bg-background flex items-center justify-center">
            <div className="font-mono text-sm text-primary animate-pulse">Loading system...</div>
        </div>
    );
}